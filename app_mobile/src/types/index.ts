export type Movie = {
    id: number,
    title: string,
    year: number,
    rating: number,
    poster: string,
    banner: string
}

export type User = {
    name: string, 
    email: string,
    password: string
}